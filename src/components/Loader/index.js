import Loader from 'react-loader-spinner'

const SpinLoader = () => (
  <div className="loader">
    <Loader type="TailSpin" color="#7094ef" height={50} width={50} />
  </div>
)
export default SpinLoader
